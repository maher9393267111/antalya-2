import { SwiperOptions } from 'swiper';

const swiperConfig = {
    modules: [
        'FreeMode', // Enable free mode
        'Navigation', // Enable navigation arrows
        'Thumbs', // Enable thumbs gallery
      ],
};

export default swiperConfig;